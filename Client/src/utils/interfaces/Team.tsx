import {Player} from "./Player";

export interface Team {
    goalKeeper: Player,
    fullback1: Player,
    fullback2: Player,

    halfback1: Player,
    halfback2: Player,
    halfback3: Player,

    forward1: Player,
    forward2: Player,
    forward3: Player,
    forward4: Player,
    forward5: Player,
}
